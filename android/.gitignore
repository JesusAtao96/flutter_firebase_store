gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Un-ignore bootstrap files so the project builds after `git clone`.
# The default Flutter template ignores these but they MUST be committed
# for the Android build to work for other developers.
/gradle-wrapper.jar
!/gradlew
!/gradlew.bat
!/gradle/wrapper/gradle-wrapper.jar

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
